platform: linux

image_resource:
  type: docker-image
  source:
    repository: zsparks1/javagcloudmaven
    tag: latest

inputs:
- name: code-repo
run:
  dir: code-repo
  path: ConcourseTestingExample/CI/scripts/test.sh #should error!