platform: linux

image_resource:
  type: docker-image
  source:
    repository: zsparks1/javagcloudmaven
    tag: latest

inputs:
- name: ConcourseTestingExample(master)

run:
  path: ./CI/scripts/test.sh